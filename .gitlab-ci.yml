image: ruby:2.1

services: 
  - gradle
  - jdk

before_script:
  -bundle install

desktop:
  script:
    - gradle desktop:build

checkstyle:
  script:
    - ./executeCheckstyleTest.sh

sonar:
  script:
    - gradle sonar
  only:
      - master
